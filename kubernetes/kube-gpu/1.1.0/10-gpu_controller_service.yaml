apiVersion: v1
kind: Service
metadata:
  labels:
    control-plane: gpu-controller
  name: gpu-controller-service
  namespace: gpu-system
spec:
  type: ClusterIP
  ports:
    - name: grpc
      port: 8080
      protocol: TCP
      targetPort: grpc
  selector:
    control-plane: gpu-controller